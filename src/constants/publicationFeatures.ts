/**
 * When true, publication preview/export sorts umoor sections by each umoor's
 * order_preference from the Umoor admin table.
 *
 * When false (current default), blocks render in editor insertion order.
 */
export const USE_UMOOR_ORDER_PREFERENCE = false;
